import 'package:flutter/material.dart';

getSizedBoxVertical({double? height}) {
  return SizedBox(height: height ?? 10);
}

getSizedBoxHorizontal({double? width}) {
  return SizedBox(width: width ?? 10);
}